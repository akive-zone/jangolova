# Basic Dynamic Scene

Add `JangolovaSampleBootstrap` to an empty GameObject. It creates a camera and
light when needed and attaches the bridge. Build the project as a desktop
player. Start that player through Xallet or the native system with the bridge
endpoint, token, and protocol supplied by Jangolova. Jangolova owns interaction
through the bridge; the launcher continues to own the player and its display.
